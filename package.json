{
  "name": "rev",
  "version": "1.0.0",
  "description": "md",
  "main": "../src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "luxu01",
  "license": "ISC",
  "devDependencies": {
    "webpack": "^5.75.0",
    "webpack-cli": "^5.0.1"
  },
  "scripts": {
    "build": "../node_modules/.bin/webpack --output-filename=bundle.js --mode=development"
  }
}
